[package]
name = "crucible-web"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "Loopback web camera. GET proxy, backlog, chat. POST /act/go only spawns."

[dependencies]
crucible-contract = { path = "../contract" }
crucible-http = { path = "../http" }
serde_json = "1"
