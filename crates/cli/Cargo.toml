[package]
name = "crucible-cli"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "Query CLI plus minimal foreground go (bin name: crucible under target/)"

[[bin]]
name = "crucible"
path = "src/main.rs"

[dependencies]
crucible-contract = { path = "../contract" }
crucible-kernel = { path = "../kernel" }

[dev-dependencies]
crucible-contract = { path = "../contract" }
serde_json = "1"
