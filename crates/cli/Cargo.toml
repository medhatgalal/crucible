[package]
name = "crucible-cli"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "Read-only query CLI for crucible.walk/v1 (bin name: crucible under target/)"

[[bin]]
name = "crucible"
path = "src/main.rs"

[dependencies]
crucible-contract = { path = "../contract" }

[dev-dependencies]
serde_json = "1"
