[package]
name = "crucible-kernel"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "FLOOR/TRACE/EVENTS writers, foreground go (QUESTIONS then MAP-HUMAN then independence then injected next_red), invoke/run, git worktree mint/remove, NEXT RED keep-on-failure, close_walk, STOP-ASK QUESTIONS/MAP-HUMAN, INDEPENDENCE_UNAVAILABLE (no HTTP)"

[dependencies]
crucible-contract = { path = "../contract" }
