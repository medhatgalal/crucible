[package]
name = "crucible-kernel"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "FLOOR/TRACE/EVENTS writers, foreground go, and invoke/run (no HTTP)"

[dependencies]
crucible-contract = { path = "../contract" }
