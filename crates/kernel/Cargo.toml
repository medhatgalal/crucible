[package]
name = "crucible-kernel"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "FLOOR/TRACE/EVENTS writers for the working-mode kernel (no HTTP, no go loop yet)"

[dependencies]
crucible-contract = { path = "../contract" }
