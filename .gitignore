# machine-specific: which CLIs and models exist on THIS box
agents.tsv

# per-run state. A program lives outside the template; if you run one here, it stays local.
items/
STATE.md
CLAIMS.md
BACKLOG.md
LESSONS.md

.DS_Store

# editor and local tooling. Skill views under .kiro/skills are source (Kiro CLI
# loads them ahead of ~/.kiro/skills). Other .kiro state stays local.
.kiro/*
!.kiro/skills/
.vscode/
.idea/
docs/history/

# A program run against the engine itself is scratch: shipping a copy of the engine
# inside the engine is absurd. Its lessons are hand-lifted into LESSONS or the changelog.
.crucible/

# local SDD ledger and isolated worktrees (not source)
.superpowers/
.worktrees/

# session research artifacts live outside the repo
RETROSPECTIVE.md
reviews/
surveys/
decisions/

# reproducible release output; published assets are not source
dist/

# design WIP; landed decisions live in architecture/adr/
architecture/wip/

# Rust workspace build
target/
